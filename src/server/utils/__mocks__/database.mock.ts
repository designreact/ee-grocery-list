export const addItems = (): Promise<string[]> => Promise.resolve(['added items']);
export const getItems = (): Promise<string[]> => Promise.resolve(['items']);
export const updateItems = (): Promise<string[]> => Promise.resolve(['updated items']);
export const deleteItems = (): Promise<string[]> => Promise.resolve(['deleted items']);